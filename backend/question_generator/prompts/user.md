Generate {count} multiple-choice questions for a movie recommendation quiz.
Respond with a JSON object matching the schema exactly.
Each question must have an id (1-{count}), a text field, and an options list of 4 strings.
